# Development settings
DEBUG=True
SECRET_KEY=django-insecure-change-this-in-production-please-make-it-random-and-long
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (leave empty for SQLite in development)
# DATABASE_URL=postgresql://user:password@localhost:5432/digno

# CORS settings for development
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Stripe settings (add your keys when implementing donations)
STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Email settings (for development, uses console backend)
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend